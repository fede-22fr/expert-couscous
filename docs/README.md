# AI Agent System

## Overview
This project is an AI agent system consisting of a backend and a frontend.

## Installation
To install dependencies, run:
```
pip install -r backend/requirements.txt
```

## Usage
Start the backend server:
```
python backend/app.py
```

Open `frontend/index.html` in your browser to view the application.